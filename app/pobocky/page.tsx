import type { Metadata } from "next";
import { offices } from "@/lib/site";
import { CtaBand } from "@/components/CtaBand";
export const metadata: Metadata = { title: "Pobočky", description: "Kontaktní místa FISPOGROUP v Praze, Brně a Opavě.", alternates: { canonical: "/pobocky" } };
export default function Page() { return <><section className="page-hero compact-hero"><p className="eyebrow"><span />Jsme vám nablízku</p><h1>Tři pobočky.<br /><em>Celá republika.</em></h1><p>Technologie, servis i konzultace dostupné z Prahy, Brna a Opavy.</p></section><section className="offices-page">{offices.map((office, index) => <article key={office.city}><span>0{index + 1}</span><div><p>{office.city}</p><h2>{office.address}</h2><small>{office.note}</small></div><div><a href={`tel:${office.phone.replaceAll(" ", "")}`}>{office.phone}</a><a href={`mailto:${office.email}`}>{office.email}</a><a href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(office.address)}`} target="_blank" rel="noreferrer">Otevřít mapu ↗</a></div></article>)}<p className="verification-note">Před ostrým spuštěním doporučujeme potvrdit číslo popisné opavské pobočky; veřejné zdroje uvádějí varianty 154 a 218.</p></section><CtaBand /></>; }
